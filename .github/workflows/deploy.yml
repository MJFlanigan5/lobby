name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Deploy to /opt/lobby
        run: |
          cd /opt/lobby
          git pull
          docker compose build --no-cache
          docker compose up -d
